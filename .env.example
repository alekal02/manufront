# Desenvolvimento (proxy Vite):
VITE_API_URL=/api

# Produção (Vercel) — URL do Render terminando com /api:
# NÃO use manuback-*.vercel.app (Flask não roda bem na Vercel)
# VITE_API_URL=https://manucontrol-backend.onrender.com/api
